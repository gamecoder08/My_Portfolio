/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
        domains: ['cdn-icons-png.flaticon.com'], // { changed code }
    },
};

export default nextConfig;
